{
  "installCommand": "rm -rf node_modules && npm install",
  "buildCommand": "npx prisma generate && next build --webpack",
  "functions": {
    "src/app/**/*": {
      "memory": 256,
      "maxDuration": 15
    },
    "src/app/api/**/*": {
      "memory": 256,
      "maxDuration": 15
    }
  }
}
